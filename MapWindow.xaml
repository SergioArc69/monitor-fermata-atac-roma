<Window x:Class="MonitorFermataAtacRoma.MapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Icon="Assets/bus.ico"
        Title="Mappa fermate" Height="640" Width="840"
        WindowStartupLocation="CenterOwner"
        Closing="MapWindow_Closing">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="InstructionTextBlock" Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap"
                   Text="Caricamento mappa..."/>

        <Border Grid.Row="1" BorderBrush="#FFCCCCCC" BorderThickness="1">
            <wv2:WebView2 x:Name="MapWebView"/>
        </Border>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ItemsControl x:Name="BusStatusList" Grid.Column="0" VerticalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFF0F0F0" CornerRadius="4" Padding="6,3" Margin="0,0,6,4">
                            <TextBlock Text="{Binding}" FontSize="12"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button x:Name="CloseButton" Grid.Column="1" Content="Chiudi" VerticalAlignment="Top"
                    Padding="16,6" Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>
